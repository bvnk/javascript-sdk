/**
 * BVNK API Endpoints
 * The BVNK API is designed to facilitate seamless and secure transactions including payments, channels, and digital wallet transactions.
 *
 * The version of the OpenAPI document: 1.0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AccountMethodDto from './AccountMethodDto';

/**
 * The PayOutMethodDto model module.
 * @module model/PayOutMethodDto
 * @version 1.0.1
 */
class PayOutMethodDto {
    /**
     * Constructs a new <code>PayOutMethodDto</code>.
     * @alias module:model/PayOutMethodDto
     */
    constructor() { 
        
        PayOutMethodDto.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>PayOutMethodDto</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PayOutMethodDto} obj Optional instance to populate.
     * @return {module:model/PayOutMethodDto} The populated <code>PayOutMethodDto</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PayOutMethodDto();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('code')) {
                obj['code'] = ApiClient.convertToType(data['code'], 'String');
            }
            if (data.hasOwnProperty('currency')) {
                obj['currency'] = ApiClient.convertToType(data['currency'], 'String');
            }
            if (data.hasOwnProperty('accountMethods')) {
                obj['accountMethods'] = ApiClient.convertToType(data['accountMethods'], [AccountMethodDto]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PayOutMethodDto</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PayOutMethodDto</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['code'] && !(typeof data['code'] === 'string' || data['code'] instanceof String)) {
            throw new Error("Expected the field `code` to be a primitive type in the JSON string but got " + data['code']);
        }
        // ensure the json data is a string
        if (data['currency'] && !(typeof data['currency'] === 'string' || data['currency'] instanceof String)) {
            throw new Error("Expected the field `currency` to be a primitive type in the JSON string but got " + data['currency']);
        }
        if (data['accountMethods']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['accountMethods'])) {
                throw new Error("Expected the field `accountMethods` to be an array in the JSON data but got " + data['accountMethods']);
            }
            // validate the optional field `accountMethods` (array)
            for (const item of data['accountMethods']) {
                AccountMethodDto.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * The ID of the pay out.
 * @member {Number} id
 */
PayOutMethodDto.prototype['id'] = undefined;

/**
 * The code of the pay out.
 * @member {String} code
 */
PayOutMethodDto.prototype['code'] = undefined;

/**
 * The currency of the pay out.
 * @member {String} currency
 */
PayOutMethodDto.prototype['currency'] = undefined;

/**
 * @member {Array.<module:model/AccountMethodDto>} accountMethods
 */
PayOutMethodDto.prototype['accountMethods'] = undefined;






export default PayOutMethodDto;

