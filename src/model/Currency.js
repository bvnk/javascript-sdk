/**
 * BVNK API Endpoints
 * The BVNK API is designed to facilitate seamless and secure transactions including payments, channels, anddigital wallet transactions.
 *
 * The version of the OpenAPI document: 1.0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CurrencyOptions from './CurrencyOptions';
import CurrencyProtocol from './CurrencyProtocol';
import ExternalCurrencyWithdrawalParameter from './ExternalCurrencyWithdrawalParameter';

/**
 * The Currency model module.
 * @module model/Currency
 * @version 1.0.1
 */
class Currency {
    /**
     * Constructs a new <code>Currency</code>.
     * @alias module:model/Currency
     */
    constructor() { 
        
        Currency.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Currency</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Currency} obj Optional instance to populate.
     * @return {module:model/Currency} The populated <code>Currency</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Currency();

            if (data.hasOwnProperty('code')) {
                obj['code'] = ApiClient.convertToType(data['code'], 'String');
            }
            if (data.hasOwnProperty('depositFee')) {
                obj['depositFee'] = ApiClient.convertToType(data['depositFee'], 'Number');
            }
            if (data.hasOwnProperty('fiat')) {
                obj['fiat'] = ApiClient.convertToType(data['fiat'], 'Boolean');
            }
            if (data.hasOwnProperty('icon')) {
                obj['icon'] = ApiClient.convertToType(data['icon'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('options')) {
                obj['options'] = CurrencyOptions.constructFromObject(data['options']);
            }
            if (data.hasOwnProperty('pricePrecision')) {
                obj['pricePrecision'] = ApiClient.convertToType(data['pricePrecision'], 'Number');
            }
            if (data.hasOwnProperty('protocols')) {
                obj['protocols'] = ApiClient.convertToType(data['protocols'], [CurrencyProtocol]);
            }
            if (data.hasOwnProperty('quantityPrecision')) {
                obj['quantityPrecision'] = ApiClient.convertToType(data['quantityPrecision'], 'Number');
            }
            if (data.hasOwnProperty('supportsDeposits')) {
                obj['supportsDeposits'] = ApiClient.convertToType(data['supportsDeposits'], 'Boolean');
            }
            if (data.hasOwnProperty('supportsWithdrawals')) {
                obj['supportsWithdrawals'] = ApiClient.convertToType(data['supportsWithdrawals'], 'Boolean');
            }
            if (data.hasOwnProperty('withdrawalFee')) {
                obj['withdrawalFee'] = ApiClient.convertToType(data['withdrawalFee'], 'Number');
            }
            if (data.hasOwnProperty('withdrawalParameters')) {
                obj['withdrawalParameters'] = ApiClient.convertToType(data['withdrawalParameters'], [ExternalCurrencyWithdrawalParameter]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Currency</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Currency</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['code'] && !(typeof data['code'] === 'string' || data['code'] instanceof String)) {
            throw new Error("Expected the field `code` to be a primitive type in the JSON string but got " + data['code']);
        }
        // ensure the json data is a string
        if (data['icon'] && !(typeof data['icon'] === 'string' || data['icon'] instanceof String)) {
            throw new Error("Expected the field `icon` to be a primitive type in the JSON string but got " + data['icon']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // validate the optional field `options`
        if (data['options']) { // data not null
          CurrencyOptions.validateJSON(data['options']);
        }
        if (data['protocols']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['protocols'])) {
                throw new Error("Expected the field `protocols` to be an array in the JSON data but got " + data['protocols']);
            }
            // validate the optional field `protocols` (array)
            for (const item of data['protocols']) {
                CurrencyProtocol.validateJSON(item);
            };
        }
        if (data['withdrawalParameters']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['withdrawalParameters'])) {
                throw new Error("Expected the field `withdrawalParameters` to be an array in the JSON data but got " + data['withdrawalParameters']);
            }
            // validate the optional field `withdrawalParameters` (array)
            for (const item of data['withdrawalParameters']) {
                ExternalCurrencyWithdrawalParameter.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * @member {String} code
 */
Currency.prototype['code'] = undefined;

/**
 * @member {Number} depositFee
 */
Currency.prototype['depositFee'] = undefined;

/**
 * @member {Boolean} fiat
 * @default false
 */
Currency.prototype['fiat'] = false;

/**
 * @member {String} icon
 */
Currency.prototype['icon'] = undefined;

/**
 * @member {Number} id
 */
Currency.prototype['id'] = undefined;

/**
 * @member {String} name
 */
Currency.prototype['name'] = undefined;

/**
 * @member {module:model/CurrencyOptions} options
 */
Currency.prototype['options'] = undefined;

/**
 * @member {Number} pricePrecision
 * @default 5
 */
Currency.prototype['pricePrecision'] = 5;

/**
 * @member {Array.<module:model/CurrencyProtocol>} protocols
 */
Currency.prototype['protocols'] = undefined;

/**
 * @member {Number} quantityPrecision
 * @default 5
 */
Currency.prototype['quantityPrecision'] = 5;

/**
 * @member {Boolean} supportsDeposits
 * @default false
 */
Currency.prototype['supportsDeposits'] = false;

/**
 * @member {Boolean} supportsWithdrawals
 * @default false
 */
Currency.prototype['supportsWithdrawals'] = false;

/**
 * @member {Number} withdrawalFee
 */
Currency.prototype['withdrawalFee'] = undefined;

/**
 * @member {Array.<module:model/ExternalCurrencyWithdrawalParameter>} withdrawalParameters
 */
Currency.prototype['withdrawalParameters'] = undefined;






export default Currency;

