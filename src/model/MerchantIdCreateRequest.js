/**
 * BVNK API Endpoints
 * The BVNK API is designed to facilitate seamless and secure transactions including payments, channels, and digital wallet transactions.
 *
 * The version of the OpenAPI document: 1.0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import MerchantIdCreateRequestWallet from './MerchantIdCreateRequestWallet';

/**
 * The MerchantIdCreateRequest model module.
 * @module model/MerchantIdCreateRequest
 * @version 1.0.1
 */
class MerchantIdCreateRequest {
    /**
     * Constructs a new <code>MerchantIdCreateRequest</code>.
     * @alias module:model/MerchantIdCreateRequest
     * @param displayName {String} The name of the merchant that will be displayed on the payments page.
     * @param wallet {module:model/MerchantIdCreateRequestWallet} 
     */
    constructor(displayName, wallet) { 
        
        MerchantIdCreateRequest.initialize(this, displayName, wallet);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, displayName, wallet) { 
        obj['displayName'] = displayName || 'Test Merchant Name';
        obj['wallet'] = wallet;
    }

    /**
     * Constructs a <code>MerchantIdCreateRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MerchantIdCreateRequest} obj Optional instance to populate.
     * @return {module:model/MerchantIdCreateRequest} The populated <code>MerchantIdCreateRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MerchantIdCreateRequest();

            if (data.hasOwnProperty('displayName')) {
                obj['displayName'] = ApiClient.convertToType(data['displayName'], 'String');
            }
            if (data.hasOwnProperty('webhookUrl')) {
                obj['webhookUrl'] = ApiClient.convertToType(data['webhookUrl'], 'String');
            }
            if (data.hasOwnProperty('wallet')) {
                obj['wallet'] = MerchantIdCreateRequestWallet.constructFromObject(data['wallet']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MerchantIdCreateRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MerchantIdCreateRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of MerchantIdCreateRequest.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['displayName'] && !(typeof data['displayName'] === 'string' || data['displayName'] instanceof String)) {
            throw new Error("Expected the field `displayName` to be a primitive type in the JSON string but got " + data['displayName']);
        }
        // ensure the json data is a string
        if (data['webhookUrl'] && !(typeof data['webhookUrl'] === 'string' || data['webhookUrl'] instanceof String)) {
            throw new Error("Expected the field `webhookUrl` to be a primitive type in the JSON string but got " + data['webhookUrl']);
        }
        // validate the optional field `wallet`
        if (data['wallet']) { // data not null
          MerchantIdCreateRequestWallet.validateJSON(data['wallet']);
        }

        return true;
    }


}

MerchantIdCreateRequest.RequiredProperties = ["displayName", "wallet"];

/**
 * The name of the merchant that will be displayed on the payments page.
 * @member {String} displayName
 * @default 'Test Merchant Name'
 */
MerchantIdCreateRequest.prototype['displayName'] = 'Test Merchant Name';

/**
 * The URL that will recieve the webhooks.
 * @member {String} webhookUrl
 * @default 'https://www.URL.com/to/send/webhooks/to'
 */
MerchantIdCreateRequest.prototype['webhookUrl'] = 'https://www.URL.com/to/send/webhooks/to';

/**
 * @member {module:model/MerchantIdCreateRequestWallet} wallet
 */
MerchantIdCreateRequest.prototype['wallet'] = undefined;






export default MerchantIdCreateRequest;

